[build-system]
requires = ["setuptools>=65.0"]

[project]
name = "universal_llm_conversation"
version = "0.1.0"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
timeout = 30
addopts = "-vv --cov=custom_components.universal_llm_conversation --cov-report=term-missing"
